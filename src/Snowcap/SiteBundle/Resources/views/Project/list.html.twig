{% if projects | length > 0 %}
    {% import "SnowcapSiteBundle::macros.html.twig" as helper %}
    <ul>
        {% for project in projects %}
            <li{{ loop.index % 3 == 0 ? ' class="three"' : '' }}>
                <a class="visual visual-{{ format }} flipper" href="{{ path('snwcp_site_project_show', {'slug': project.slug}) }}" title="{{ "project.link.title" | trans({'%title%': project.title}) }}">
                    <span>{{project.title}}</span>{{ helper.project_image_flipper(project, format) }}
                </a>
            </li>
        {% endfor %}
    </ul>
{% else %}
    <p>{{ "project.latest.empty" | trans }}</p>
{% endif %}