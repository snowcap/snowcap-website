{% if technologies|length > 0 %}
    <ul>
        {% for technology in technologies %}
            <li><a href="{{ url('snwcp_site_technology_show', { 'slug': technology.slug }) }}">{{ technology.name }}</a></li>
        {% endfor %}
    </ul>
{% endif %}