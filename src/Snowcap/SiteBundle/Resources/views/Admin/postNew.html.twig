{% extends "SnowcapSiteBundle:Admin:base.html.twig" %}
{% block body %}
<h1>Post creation</h1>
<form action="{{ path('admin_posts_create') }}" method="post" {{ form_enctype(form) }}>
	{{ form_widget(form) }}
	<p>
		<button type="submit">
			Create
		</button>
	</p>
</form>
<ul class="record_actions">
	<li>
		<a href="{{ path('admin_posts') }}"> Back to the list</a>
	</li>
</ul>
{% endblock %}